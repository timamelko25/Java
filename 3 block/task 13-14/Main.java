public class Main {
    public static void main(String[] args) {
        // Вариант 1: Вызов через имя класса
        A.printVars(); // Вызов статического метода через имя класса

        // Вариант 2: Вызов через объект класса (не рекомендуется)
        A aObj = new A(); // Создание объекта класса
        aObj.printVars(); // Вызов статического метода через объект

        // Вариант 3: Вызов из другого статического метода
        A.anotherMethod(); // Вызов через другой статический метод класса A
    }
}